\subsection{Homography for panoramic stitching}\label{subsec:homography}

\question{Write down the homography matrix and tell whether these values look reasonable
    (especially the large and small values might raise questions).}

\question{What is the difference between the “input img 1\&2” and “Reg. Image1\&2” in the Output Images menu?}

\question{Give snapshots of the code where the main processing steps explained in the theory lessons occur, 
    especially for the homography estimation and RANSAC.}

\question{How does the algorithm know that it has converged to a good (or good enough) solution? What is the error
function, exactly?}

\question{Capture 4 images for yourself and stitch them together into a Panoramic image using this program multiple
times for each successive pair of images. Show the intermediate results of inliers and outliers, the homography
matrices and the stitched image.}
